name: Publish to PyPI (Trusted Publishing)

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to publish (e.g., 2.0.8)'
        required: true
        type: string

permissions:
  contents: read
  id-token: write  # REQUIRED for trusted publishing
  actions: write

jobs:
  ensure-tests-running:
    name: Ensure Build Tests Are Running
    runs-on: ubuntu-latest
    outputs:
      run_id: ${{ steps.find-or-trigger.outputs.run_id }}
    
    steps:
      - name: Find existing or trigger new build
        id: find-or-trigger
        uses: actions/github-script@v7
        with:
          script: |
            const workflow_id = 'cross-platform-build-verification.yml';
            const ref = context.eventName === 'release' 
              ? context.payload.release.tag_name 
              : context.ref;
            
            console.log(`🔍 Looking for builds for ref: ${ref} (SHA: ${context.sha})`);
            
            // 1. Get ALL recent runs (don't filter by status yet)
            const runs = await github.rest.actions.listWorkflowRuns({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: workflow_id,
              per_page: 20
            });
            
            // 2. Filter client-side for ANY active status (Queued OR In Progress)
            const activeStatuses = ['queued', 'in_progress', 'waiting', 'requested', 'pending'];
            
            let targetRun = runs.data.workflow_runs.find(run => 
              (run.head_sha === context.sha || run.head_branch === ref) &&
              activeStatuses.includes(run.status)
            );
            
            if (targetRun) {
              console.log(`✅ Found existing active build: ${targetRun.id}`);
              console.log(`   Status: ${targetRun.status}`);
              core.setOutput('run_id', targetRun.id);
              return;
            }
            
            // 3. If release event, wait loop (checking for Queued as well)
            if (context.eventName === 'release') {
              console.log('📦 Release event - waiting for auto-triggered build to appear...');
              
              for (let i = 0; i < 12; i++) { // Wait up to 1 minute
                await new Promise(resolve => setTimeout(resolve, 5000));
                
                const refreshRuns = await github.rest.actions.listWorkflowRuns({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  workflow_id: workflow_id,
                  per_page: 20
                });
                
                targetRun = refreshRuns.data.workflow_runs.find(run => 
                  run.head_sha === context.sha &&
                  activeStatuses.includes(run.status)
                );
                
                if (targetRun) {
                  console.log(`✅ Found auto-triggered build: ${targetRun.id} (${targetRun.status})`);
                  core.setOutput('run_id', targetRun.id);
                  return;
                }
              }
              throw new Error('❌ Release should have auto-triggered build, but none found');
            }
            
            // 4. Manual trigger - create new build
            console.log('🔧 Manual workflow_dispatch - triggering new build...');
            
            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: workflow_id,
              ref: ref
            });
            
            console.log('Waiting for new build to appear...');
            
            // Wait for the triggered build to show up
            for (let i = 0; i < 24; i++) { // Wait up to 2 minutes
              await new Promise(resolve => setTimeout(resolve, 5000));
              
              const newRuns = await github.rest.actions.listWorkflowRuns({
                owner: context.repo.owner,
                repo: context.repo.repo,
                workflow_id: workflow_id,
                per_page: 10
              });
              
              // We look for the one we just triggered (latest non-completed)
              const latestRun = newRuns.data.workflow_runs.find(run => 
                activeStatuses.includes(run.status)
              );
              
              if (latestRun) {
                console.log(`✅ New build started: ${latestRun.id} (${latestRun.status})`);
                core.setOutput('run_id', latestRun.id);
                return;
              }
            }
            
            throw new Error('❌ Failed to find triggered build');
            
  wait-for-tests:
    name: Wait for Build Completion
    needs: ensure-tests-running
    runs-on: ubuntu-latest
    
    steps:
      - name: Wait for build workflow (INFINITE - NO TIMEOUT)
        uses: actions/github-script@v7
        with:
          script: |
            const run_id = '${{ needs.ensure-tests-running.outputs.run_id }}';
            const intervalMs = 30000; // Check every 30 seconds
            
            console.log(`🔍 Monitoring build run ID: ${run_id}`);
            console.log(`⏰ Will wait INDEFINITELY until build completes (no timeout)`);
            
            let iteration = 0;
            
            while (true) {
              iteration++;
              
              const run = await github.rest.actions.getWorkflowRun({
                owner: context.repo.owner,
                repo: context.repo.repo,
                run_id: run_id
              });
              
              const elapsed = Math.floor((iteration * intervalMs) / 60000);
              console.log(`[${elapsed}m] Status: ${run.data.status} | Conclusion: ${run.data.conclusion || 'N/A'}`);
              
              if (run.data.status === 'completed') {
                if (run.data.conclusion === 'success') {
                  console.log('✅ All build tests passed!');
                  return;
                } else {
                  throw new Error(`❌ Build failed with conclusion: ${run.data.conclusion}`);
                }
              }
              
              // Still running - keep waiting
              if (run.data.status === 'queued') {
                console.log('   ⏳ Build is queued, waiting for runner...');
              } else if (run.data.status === 'in_progress') {
                console.log('   🏃 Build is running...');
              }
              
              await new Promise(resolve => setTimeout(resolve, intervalMs));
            }

  deploy:
    needs: wait-for-tests
    runs-on: ubuntu-latest
    environment: pypi  # This is REQUIRED for trusted publishing
    permissions:
      id-token: write  # MANDATORY for trusted publishing
      contents: read
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        ref: ${{ github.ref }}
      
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install build tomli
        
    - name: Verify package version
      run: |
        PACKAGE_VERSION=$(python -c "import tomli; print(tomli.load(open('pyproject.toml', 'rb'))['project']['version'])")
        
        if [ "${{ github.event_name }}" == "workflow_dispatch" ]; then
          EXPECTED_VERSION="${{ inputs.version }}"
          # Strip the 'v' prefix if present
          EXPECTED_VERSION="${EXPECTED_VERSION#v}"
        else
          TAG_VERSION="${{ github.ref_name }}"
          EXPECTED_VERSION=${TAG_VERSION#v}
        fi
        
        echo "Package version: $PACKAGE_VERSION"
        echo "Expected version: $EXPECTED_VERSION"
        
        if [ "$PACKAGE_VERSION" != "$EXPECTED_VERSION" ]; then
          echo "❌ Version mismatch!"
          exit 1
        fi
    
    echo "✅ Version verified: $PACKAGE_VERSION"
        
    - name: Install build tools
      run: |
        python -m pip install --upgrade pip
        pip install build tomli cibuildwheel

    - name: Set up QEMU (for ARM64 builds on x64 runners)
      uses: docker/setup-qemu-action@v3
      with:
        platforms: arm64
    
    - name: Build wheels with cibuildwheel
      uses: pypa/cibuildwheel@v2.20.0
      env:
        CIBW_BUILD: cp310-* cp311-* cp312-* cp313-*
        CIBW_SKIP: "*-musllinux_*"
        CIBW_ARCHS_LINUX: x86_64 aarch64
        CIBW_ARCHS_MACOS: x86_64 arm64
        CIBW_ARCHS_WINDOWS: AMD64
        CIBW_ENVIRONMENT: "OMNIPKG_SKIP_C_EXT=0"
        
    - name: Build source distribution
      run: |
        python -m build --sdist
        
    - name: Publish to PyPI using Trusted Publishing
      uses: pypa/gh-action-pypi-publish@release/v1
      # NOTE: No password or token needed! Trusted publishing handles auth automatically
    
    - name: Success
      run: |
        echo "🎉 Published to PyPI using Trusted Publishing!"
        echo "📦 Package: omnipkg"
        echo "🔐 Authentication: OIDC (no tokens used)"
